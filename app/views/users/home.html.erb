<div class="container">
<div class="row">
<div class="col-lg-1"></div>
<div class="col-lg-3">
<div class="user-info">
	  <h2>User info</h2>
    <!-- :fillでは、縦横の大きさを指定しています。単位はpxです。refileの仕様により、_idを外した状態でカラムを指定しています。 -->
    <%= attachment_image_tag @user, :user_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
    <div class="user-name-box">
      <div class="user-name-title">
        <p class="name">name</p>
      </div>
      <div class="user-name-current">
        <p class="name"><%= @user.name %></p>
      </div>
    </div>
    <div class="user-introduction-box">
      <div class="user-introduction-title">
        <p>introduction</p>
      </div>
      <div class="user-introduction-current">
        <!-- ↓中央寄せ左頭合わせになっているか確認した方が良い -->
        <p calss="introduction"><%= current_user.introduction %></p>
      </div>
    </div>
    <div class="user-introduction"></div>
    <div class="user-info-edit">
         <button type="button" class="btn btn-default btn-sm wrench"><span class="glyphicon glyphicon-wrench"></span></button>
    </div>
</div>
</div>
<div class="col-lg-8"></div>
<table class="table table-striped">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Opinion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= attachment_image_tag @user, :user_image, :fill, 5, 5, format: 'jpeg', fallback: "no_image.jpg" %></td>
        <td><%= book.title %></td>
        <td><%= book.body %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<div class="row">
<div class="col-lg-1"></div>
<div class="col-lg-3">
<h2>New book</h2>
<%= form_for (@book) do |f| %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :opinion %><br>
    <%= f.text_area :body %>
  </p>

  <p>
    <%= f.submit "Create book"%>
  </p>
<% end %>
</div>
<div class="col-lg-8"></div>
</div>
</div>

